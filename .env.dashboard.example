# =========================================
# CONFIGURACIÓN DASHBOARD WEB - VARIABLES DE ENTORNO
# =========================================

# ===== CONFIGURACIÓN BÁSICA DE RED =====
DASHBOARD_PORT=3000
DASHBOARD_HOST=0.0.0.0    # 0.0.0.0 = permite conexiones externas, localhost = solo local

# ===== CONFIGURACIÓN SSL/HTTPS =====
DASHBOARD_SSL_ENABLED=false
DASHBOARD_SSL_KEY_PATH=./certs/dashboard-key.pem
DASHBOARD_SSL_CERT_PATH=./certs/dashboard-cert.pem
DASHBOARD_SSL_CA_PATH=./certs/dashboard-ca.pem

# ===== CONFIGURACIÓN SOCKET.IO =====
DASHBOARD_CORS_ORIGIN=*                           # "*" = cualquier origen, "https://mi-dominio.com" para específico
DASHBOARD_CORS_CREDENTIALS=false
DASHBOARD_SOCKET_TRANSPORTS=websocket,polling     # Transportes separados por coma
DASHBOARD_PING_TIMEOUT=60000                      # ms - timeout para ping
DASHBOARD_PING_INTERVAL=25000                     # ms - intervalo entre pings

# ===== CONFIGURACIÓN PÚBLICA =====
DASHBOARD_PUBLIC_URL=                             # Opcional: https://mi-dominio.com/dashboard
DASHBOARD_BASE_PATH=/                             # Ruta base del dashboard

# ===== CONFIGURACIÓN DE PROXY =====
DASHBOARD_TRUST_PROXY=false                      # true si está detrás de proxy (nginx, apache, cloudflare)

# ===== CONFIGURACIÓN DE SEGURIDAD =====
DASHBOARD_SECURITY_HELMET=true                   # Headers de seguridad
DASHBOARD_RATE_LIMITING=true                     # Limitación de velocidad
DASHBOARD_MAX_REQUESTS=100                       # Máximo requests por ventana
DASHBOARD_RATE_WINDOW=900000                     # Ventana en ms (15 min = 900000)

# =========================================
# EJEMPLOS DE CONFIGURACIONES TÍPICAS
# =========================================

# DESARROLLO LOCAL:
# DASHBOARD_HOST=localhost
# DASHBOARD_PORT=3000
# DASHBOARD_SSL_ENABLED=false

# PRODUCCIÓN CON SSL:
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=443
# DASHBOARD_SSL_ENABLED=true
# DASHBOARD_PUBLIC_URL=https://dashboard.mi-empresa.com
# DASHBOARD_CORS_ORIGIN=https://mi-app.com

# DETRÁS DE NGINX/APACHE:
# DASHBOARD_HOST=127.0.0.1
# DASHBOARD_PORT=3001
# DASHBOARD_TRUST_PROXY=true
# DASHBOARD_PUBLIC_URL=https://mi-dominio.com/dashboard

# ACCESO EXTERNO SIN SSL:
# DASHBOARD_HOST=0.0.0.0
# DASHBOARD_PORT=8080
# DASHBOARD_PUBLIC_URL=http://mi-ip-publica:8080

# =========================================
# COMANDOS PARA GENERAR CERTIFICADOS SSL
# =========================================

# Para desarrollo (self-signed):
# mkdir -p certs
# openssl req -x509 -newkey rsa:4096 -keyout certs/dashboard-key.pem -out certs/dashboard-cert.pem -days 365 -nodes -subj "/C=MX/ST=Sinaloa/L=Culiacan/O=Dashboard/CN=localhost"

# Para producción:
# Usar Let's Encrypt, Cloudflare SSL, o certificados de tu proveedor