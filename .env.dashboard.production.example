# =========================================
# CONFIGURACIÓN DASHBOARD PRODUCCIÓN
# =========================================

# ===== CONFIGURACIÓN DE RED PRODUCCIÓN =====
DASHBOARD_PORT=443                                # Puerto SSL estándar
DASHBOARD_HOST=0.0.0.0                          # Permite conexiones externas

# ===== SSL/HTTPS HABILITADO =====
DASHBOARD_SSL_ENABLED=true
DASHBOARD_SSL_KEY_PATH=/etc/ssl/private/dashboard.key    # Ruta absoluta sistema
DASHBOARD_SSL_CERT_PATH=/etc/ssl/certs/dashboard.crt     # Ruta absoluta sistema
DASHBOARD_SSL_CA_PATH=/etc/ssl/certs/ca-bundle.crt      # Chain certificates

# ===== CONFIGURACIÓN PÚBLICA PRODUCCIÓN =====
DASHBOARD_PUBLIC_URL=https://dashboard.mi-empresa.com   # Tu dominio real
DASHBOARD_BASE_PATH=/

# ===== CORS RESTRINGIDO PRODUCCIÓN =====
DASHBOARD_CORS_ORIGIN=https://mi-app.com,https://admin.mi-empresa.com
DASHBOARD_CORS_CREDENTIALS=true

# ===== PROXY Y BALANCEADOR DE CARGA =====
DASHBOARD_TRUST_PROXY=true                      # Importante si usas nginx/apache/cloudflare

# ===== SEGURIDAD MÁXIMA =====
DASHBOARD_SECURITY_HELMET=true
DASHBOARD_RATE_LIMITING=true
DASHBOARD_MAX_REQUESTS=50                       # Más restrictivo en producción
DASHBOARD_RATE_WINDOW=600000                    # 10 minutos

# ===== SOCKET.IO OPTIMIZADO PRODUCCIÓN =====
DASHBOARD_SOCKET_TRANSPORTS=websocket          # Solo WebSocket en producción
DASHBOARD_PING_TIMEOUT=30000                   # Timeout más corto
DASHBOARD_PING_INTERVAL=10000                  # Pings más frecuentes

# =========================================
# CONFIGURACIÓN NGINX/APACHE FRONTAL
# =========================================

# Si usas proxy reverso, Dashboard puede correr internamente en:
# DASHBOARD_HOST=127.0.0.1
# DASHBOARD_PORT=3001
# DASHBOARD_SSL_ENABLED=false  # SSL termina en nginx

# =========================================
# RUTAS TÍPICAS DE CERTIFICADOS PRODUCCIÓN
# =========================================

# LINUX/UBUNTU:
# /etc/ssl/private/   (privadas)
# /etc/ssl/certs/     (públicas)
# /etc/letsencrypt/live/mi-dominio.com/

# CENTOS/RHEL:
# /etc/pki/tls/private/
# /etc/pki/tls/certs/

# WINDOWS:
# C:\ssl\certs\
# C:\certificados\

# DOCKER:
# /app/ssl/
# /usr/local/ssl/

# =========================================
# COMANDOS PARA CERTIFICADOS PRODUCCIÓN
# =========================================

# Let's Encrypt (recomendado):
# certbot certonly --webroot -w /var/www/html -d dashboard.mi-empresa.com
# Certificados estarán en: /etc/letsencrypt/live/dashboard.mi-empresa.com/

# Cloudflare Origin Certificate:
# Descargar desde Cloudflare Dashboard > SSL/TLS > Origin Server

# Compra de certificado comercial:
# Seguir instrucciones del proveedor (DigiCert, Comodo, etc.)

# =========================================
# EJEMPLO NGINX PROXY
# =========================================

# server {
#     listen 443 ssl http2;
#     server_name dashboard.mi-empresa.com;
#
#     ssl_certificate /etc/ssl/certs/dashboard.crt;
#     ssl_certificate_key /etc/ssl/private/dashboard.key;
#
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }